'use strict';

var cities = require('all-the-cities');
var kdbush = require('kdbush');
var geokdbush = require('./');

var index = kdbush(cities, (p) => p.lon, (p) => p.lat);

var lng = -180 + 360 * Math.random();
var lat = -90 + 180 * Math.random();
var lng = 10;
var lat = -70;
console.log(geokdbush.around(index, lng, lat, 1));

console.log(lng, lat);
